
let

  private
    const a ~ 1
  in

    private
      const b ~ a + 1 ! a visible aquí, b = 2
    in
      const c ~ b + a ! c = 3
    end
    ;
    const d ~ a + c ! d = 4
  end

in

  putint(c) ! c debe ser visible aquí, imprime 3

end